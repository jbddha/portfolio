import zombiedice, random

#A few test bots, to play 'Zombie Dice Bots' simulator
#Uncomment after their description to see how they stack up against other bots
#There is a zombie who acts randomly, a zombie who stops rolling once they see 2 brains
#A zombie who stops rolling when they see 2 shotguns
#A zombie who decides how much they will roll before their turn but stops if they sees 2 shotguns
#A zombie who will stop rolling if they roll more shotguns than brains


class MyZombie:
    def __init__(self, name):
        # All zombies must have a name:
        self.name = name

    def turn(self, gameState):
        # gameState is a dict with info about the current state of the game.

        diceRollResults = zombiedice.roll() # first roll
        # roll() returns a dictionary with keys 'brains', 'shotgun', and
        # 'footsteps' with how many rolls of each type there were.
        # The 'rolls' key is a list of (color, icon) tuples with the
        # exact roll result information.
        # Example of a roll() return value:
        # {'brains': 1, 'footsteps': 1, 'shotgun': 1,
        #  'rolls': [('yellow', 'brains'), ('red', 'footsteps'),
        #            ('green', 'shotgun')]}
        brains = 0
        #Random Zombie
        #while diceRollResults is not None:
        #    brains += diceRollResults['brains']
        #   if random.randint(0, 1) == 1:
        #        diceRollResults = zombiedice.roll() # roll again
        #    else:
        #        break
        #Stops At 2 Brains
        #while diceRollResults is not None:
        #    brains += diceRollResults['brains']
        #    howManyBrains = diceRollResults['brains']
        #    if howManyBrains >= 2:
        #        break
        #    else:
        #        diceRollResults = zombiedice.roll()
        #StopsAt2Shotguns
        #shotguns = 0
        #while diceRollResults is not None:
        #    brains += diceRollResults['brains']
        #    shotguns += diceRollResults['shotgun']
        #    if shotguns < 2:
        #        diceRollResults = zombiedice.roll()
        #    else:
        #        break
        #Rolls 1 To 4 Times But Stops Early if 2 Shotguns
        #rolls = 0
        #shotguns = 0
        #rollAmount = random.randint(1, 4)
        #while diceRollResults is not None:
        #    brains += diceRollResults['brains']
        #    shotguns += diceRollResults['shotgun']
        #    rolls += 1
        #    if shotguns < 2 and rolls <= rollAmount:
        #        diceRollResults = zombiedice.roll()
        #    else:
        #        break
        #Stops rolling if more shotguns than brians
        #shotguns = 0
        #while diceRollResults is not None:
        #    brains += diceRollResults['brains']
        #    shotguns += diceRollResults['shotgun']
        #    brainsThisTurn = diceRollResults['brains']
        #    if shotguns < brainsThisTurn:
        #        diceRollResults = zombiedice.roll()
        #    else:
        #        break

zombies = (
    zombiedice.examples.RandomCoinFlipZombie(name='Random'),
    zombiedice.examples.RollsUntilInTheLeadZombie(name='Until Leading'),
    zombiedice.examples.MinNumShotgunsThenStopsZombie(name='Stop at 2 Shotguns', minShotguns=2),
    zombiedice.examples.MinNumShotgunsThenStopsZombie(name='Stop at 1 Shotgun', minShotguns=1),
    MyZombie('MyZombie'),
    # Add any other zombie players here.
)

# Uncomment one of the following lines to run in CLI or Web GUI mode:
#zombiedice.runTournament(zombies=zombies, numGames=1000)
zombiedice.runWebGui(zombies=zombies, numGames=1000)
